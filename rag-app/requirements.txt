# Core orchestration
langchain==0.3.0
langchain-core
langchain-community==0.3.0
langchain-text-splitters==0.3.0

# Local LLM (Llama via Ollama)
langchain-ollama==0.2.0

# Embeddings (local HuggingFace models)
langchain-huggingface==0.1.0
sentence-transformers==3.0.0

# Vector and keyword search
# (FAISS itself has no Metal/GPU backend, so vector search stays CPU-bound
# regardless of torch's device — negligible at this corpus size)
faiss-cpu>=1.9
rank-bm25==0.2.2

# ML backend — plain torch (not the CPU-only build) so the embeddings and
# reranker can use Apple Metal (MPS) on Apple Silicon; also required since
# the old pinned torch==2.1.0+cpu has no wheel for Python 3.14
torch
